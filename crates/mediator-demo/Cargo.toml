[package]
name = "mediator-demo"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "mediator"
path = "src/main.rs"

[dependencies]
mediator-types = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
# wired to the rest at integration time:
# mediator-core, mediator-prover, mediator-fairdiv, mediator-llm, mediator-tui
